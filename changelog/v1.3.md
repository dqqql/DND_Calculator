# v1.3 — 2026-02-17

- 新增：`changelog/` 与 `pack_release.py` 的打包集成（打包时会把最新公告复制为 `CHANGELOG.md` 并生成 `release.json` 与 `version.txt`）。
- 新增：抽卡池（gacha）功能 —— 在规则书中新增抽卡机制并在工具中提供抽卡计算器。
- 修改：调整治疗包（治疗包数值已修改，详见规则书中的“物品”章节）。
- 优化：打包脚本现在会生成压缩包（`斗兽场v1.3.zip`）以方便分发。

已记录：v1.2 的变更已在 `changelog/v1.2.md` 中保存。

## 兼容/注意
- 打包会包含：规则书（HTML/MD）、`lib/` 依赖、`changelog/`、以及工具脚本/页面。
- 如需覆盖已存在的发布目录或压缩包，请使用 `--force`。
