# v2.0 — 2026-02-17

主要更新（v2.0）：

- 新增：两个可选怪物池（玩家在进入轮次时选择，工具仅显示所选池）
  - 第一轮可选：`百鬼夜行`（原池） / `送财童子`（新增）
  - 第二轮可选：`诸神黄昏`（原池） / `福到临头`（新增）
  - 规则：玩家在进入每轮时通过弹窗选择怪物池，工具将只显示所选池。

- 扩充怪物库（将用户提供的条目并入）：
  - 第一轮（CR ≤ 1）新增/调整：地精、侍僧（调整为 CR 1/4）、学徒法师、坐狼、豺狼人、活化盔甲、赤铜龙雏龙 等
  - 第二轮（CR 2-5）已替换为用户提供的“诸神黄昏”完整列表（新增/调整：至高妖精邪术师、食人魔、石像鬼、骑士、石化蜥蜴、枭熊、蝎尾狮、双头巨人、红龙幼龙、角斗士、巨魔、惑控师 等）

- 界面：
  - `index.html` 中新增“怪物池选择”交互（进入轮次时弹窗选择），选择结果会决定当前池子并保存到 localStorage
  - 抽卡 / 展示将以“实际使用”的池子为准

- 文档：规则书（`DND新春怪物角斗场.html`）已同步更新，包含新池说明与新怪物条目

兼容性说明：
- 现有流程未破坏；默认行为为使用玩家选择的池子。

已完成：前端代码、规则页、变更日志同步更新。